# 从未安装过的运营：首次安装 V2.6.1

把下面整段发给该运营电脑上的 Codex：

```text
请在我的电脑上首次安装 Origin Sculpture Blog Skill V2.6.1。本轮只做本机安装与验证，不创建、更新或发布 Shopify 文章，也不要改 Shopify 主题。

目标来源：
- 仓库：vickyfobes-ops/Blog-ORS-V2.4.0
- 分支：codex/origin-sculpture-blog-ops-v2-6-1
- 仓库路径：skills/origin-sculpture-blog-ops-v2-6-1
- 安装目录和调用名：origin-sculpture-blog-ops-v2-6-1
- 安装后调用：$origin-sculpture-blog-ops-v2-6-1

请使用 Codex 内置 skill-installer 从上述固定分支和路径安装。不要下载整个仓库当作 Skill，也不要覆盖其他已存在的 Skill。若同名目录已经存在，先检查版本；正确则跳过重复安装，异常则停下报告，不要删除重装。

检查 SKILL.md 的 name=origin-sculpture-blog-ops-v2-6-1、metadata.version=2.6.1，运行 skill-creator 的 quick_validate。然后调用本机 Codex workspace dependencies，使用其文档 Python 与 render_docx.py 运行新目录中的 scripts/self_test.py；必须看到 status=PASS。若是 Windows，先定位 LibreOffice 的 soffice.exe；已安装但找不到命令时可修正本次进程 PATH，未安装或渲染器不可用则报告准确缺失项并停止，不要绕过自检或擅自安装系统软件。不要套用另一台 Mac 的路径。

Shopify 凭证必须由获授权的人在这台电脑上单独配置到 Skill 文档指定的受保护位置，不要让人把 token 或 .env 发到聊天，也不要写进 Skill、GitHub 或文章目录。如果这台电脑没有凭证，本次仍可完成本机安装/自检，但不要连接或修改 Shopify；需要店铺功能时再由获授权的人配置并做只读验证。

只有自检 PASS 后，才允许用新版本处理后续正式文章。新对话中明确调用 $origin-sculpture-blog-ops-v2-6-1。它生成的 Shopify 正文不带 H1；人工发布前仍需确认同一文章模板把标题渲染成唯一 H1。任何保存草稿、发布或更新都需用户对当前文章包给出精确哈希确认码；本次安装不构成该授权。

最后只向我汇报：安装路径、调用名、quick_validate 和 self_test 的结果，以及是否缺少本机依赖或 Shopify 配置；不要输出任何凭证，不要执行 Shopify 写入。
```
